[build]
  # Comando simples que nunca falha
  command = "echo 'Site pronto!'"
  # A pasta do site (raiz)
  publish = "." 

[functions]
  # A pasta do backend
  directory = "functions"
  node_bundler = "esbuild"